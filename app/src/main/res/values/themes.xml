<resources xmlns:tools="http://schemas.android.com/tools">
    <style name="Theme.BooxNote" parent="Theme.MaterialComponents.DayNight.NoActionBar">
        <item name="colorPrimary">#000000</item>
        <item name="colorPrimaryVariant">#000000</item>
        <item name="colorOnPrimary">#FFFFFF</item>
        <!-- Match the app's white toolbar instead of a solid black bar with light (white) icons -
             that combination reads as a stale/pre-Material status bar against an otherwise white UI. -->
        <item name="android:statusBarColor" tools:targetApi="l">#FFFFFF</item>
        <item name="android:windowLightStatusBar" tools:targetApi="m">true</item>
    </style>

    <style name="Theme.BooxNote.AboutDialog" parent="Theme.AppCompat.Dialog">
        <item name="android:windowMinWidthMajor">0%</item>
        <item name="android:windowMinWidthMinor">0%</item>
        <item name="android:windowBackground">@android:color/transparent</item>
    </style>
</resources>
